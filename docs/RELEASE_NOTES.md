# Release Notes

## 0.1.0

- Started Phase 0 foundation.
- Added neutral VisionAI package structure, docs, typed contracts, state machine, event bus, logging redaction, and unit tests.
- Added initial Phase 1 capability registry, policy engine, confirmation service, and related unit tests.
- Added Phase 1 rate limiter, URL policy helper, serialized dispatcher, audit sink, and related unit tests.
- Added Phase 1 JSON permission store, JSON Lines audit sink, lock-state adapter boundary, private/local URL host blocking, and security input tests.
- Added URL IDN/redirect/host-confusion tests, confirmation replay/expiry edge tests, prompt-injection data tests, and a conservative Windows lock-state adapter wrapper.
- Added CI workflow, local verification scripts, and migration quarantine documentation.
- Initialized Git repository on `main` and added Python environment repair documentation.
- Installed Python 3.12.10, created `.venv312`, updated vulnerable dependency pins, and verified the local suite.
- Added the first real, dispatcher-wired capabilities: `system.time`, `system.date`, `system.battery`, `system.health`, all read-only.
- Pushed the repository to https://github.com/5hubhamMishra/VISIONAI; hosted CI is live and has passed on every commit so far.
- Found and fixed seven defects surfaced by a full security/correctness review of the safety-critical modules, each with a regression test: `UrlPolicy` failed open on an empty host allowlist; `WindowsLockStateAdapter` never actually detected lock state (it checked session membership, not lock status); `SerializedDispatcher` audited denials using the caller-supplied risk level instead of the manifest's; `FixedWindowRateLimiter` and `StateMachine` both mutated shared state with no lock, reproducible as real races under concurrent access; `EventBus.close()` could deadlock a consumer if the queue was full at close time; and log redaction did not work at all as wired (attached to the wrong logger) and could crash message rendering even when fixed to attach correctly.
- Migrated the first capability from `../jarvis` per `docs/MIGRATION_QUARANTINE.md`: `app.open`, opening one allowlisted desktop application (`notepad`, `calculator`, `paint`) by exact executable name with `shell=False`, deliberately excluding anything from the old prototype's broader app list that is itself a command surface (`cmd`, `powershell`, Task Manager). Verified live end to end, including a real Notepad launch and clean shutdown.
- Added `system.capabilities` and `system.help`, the Section 13 initial safe help/capability-list commands.
- Documented a concrete, traceable OS command injection path in `../jarvis` (found while reviewing `brain/intent_parser.py`): unrecognized app/site names fall back to the raw spoken word instead of being rejected, and that value reaches `subprocess.Popen(cmd, shell=True)` unvalidated.
- Locally quarantined that old `../jarvis` app/web/system execution path in this workspace. The source edits are outside the `visionai/` Git repository, so the trusted repo records the hardening but still treats the old prototype as untrusted reference material.
- Migrated safe browser/search behavior into the trusted runtime as `browser.open` and `browser.search`, both reversible and allowlist-backed. Site names map to fixed HTTPS URLs, search queries are encoded through `UrlPolicy`, and tests inject the browser opener so verification never launches a real browser.
- Migrated safe media behavior into the trusted runtime as `media.control`, a reversible allowlisted media-key capability with injected key pressing for tests.
- Added `system.stop`, backed by `OperationController`, to request cooperative cancellation of the current operation without killing threads or processes directly.
- Added `TextCommandPlanner` and `visionai --text "<command>"`: a deterministic, non-LLM text planner that maps a small set of reviewed phrases into typed action plans for the already-registered capabilities, using the same allowlists the capabilities themselves enforce. Found and fixed a real crash bug via test execution: rejected text containing a control character used to raise a pydantic `ValidationError` instead of returning the intended non-executable response, because the rejection fallback passed unsanitized text into a `SafeText`-constrained field.
- Added `EventOrchestrator`, the "glue, not authority" layer connecting a bounded input event bus to the text planner, the unmodified state machine, and the dispatcher: a text command is framed as an instant, already-final transcript and walked through the real `IDLE -> LISTENING -> TRANSCRIBING -> INTERPRETING` transition graph, with domain errors caught and published as `ErrorEvent`s and the state machine always returned to `IDLE` in a `finally` block regardless of outcome.
- Started Phase 2 (Desktop UI) with the first slice: `MainWindow`, a minimal PySide6 window (command input, run button, result display, audit history) and the `visionai-ui` console script. It adds no planning or execution logic of its own -- every typed command becomes a `TranscriptEvent` handed to the same `EventOrchestrator` the CLI uses. Tested headless via `pytest-qt` with Qt's offscreen platform plugin, set automatically by `tests/conftest.py` so it works in CI with no real display. Added `PySide6` and `pytest-qt` as dependencies.
- Added a Stop button to `MainWindow`, closing a real gap toward Phase 2's "cancellation" exit criterion: it requests cancellation the same way `visionai --text "stop"` does and, unlike the command input and Run button, is never disabled while a command runs. The first Stop-button slice still used blocking button handlers; that limitation was later addressed by the worker-thread execution slice.
- Started a WCAG 2.2 AA pass on `MainWindow` (partial, not complete). Found and fixed a real bug via test execution: the window set no initial keyboard focus at all on show. Verified Tab/Shift+Tab reach every interactive control in both directions with no keyboard trap, and associated the "Result"/"History" labels with their widgets via `QLabel.setBuddy()`. Contrast, OS scaling, and a live screen-reader pass remain unverified.
- Added a system tray icon to `MainWindow`: Show/Quit menu, click-to-toggle visibility, and minimize-to-tray on close when a tray is actually available (falls back to a normal close otherwise, so the window can never become unreachable). Pure window-lifecycle control with no path into the runtime. Live-verified on the real Windows desktop (tray available, icon visible, close-to-tray works), since the headless CI platform never reports a tray as available.
- Added early confirmation-dialog plumbing for the next Phase 2 slice: `Runtime` now exposes an injectable `ConfirmationService`, and `SerializedDispatcher.evaluate()` supports non-executing policy preflight without consuming rate-limit quota. `dispatch()` still re-evaluates policy before any handler runs.
- Added the first `MainWindow` confirmation prompt for sensitive/destructive actions: `ConfirmationRequest` output now opens a `QMessageBox`, Yes calls `EventOrchestrator.confirm()`, and No cancels the pending confirmation without executing. Tested with a synthetic sensitive capability; no real sensitive capability is registered yet.
- Added a read-only Diagnostics dialog to `MainWindow` and moved command/confirmation execution into a Qt worker thread so the GUI event loop stays responsive. Busy Stop now signals `OperationController` directly instead of waiting behind the serialized dispatcher; idle Stop still uses the audited `system.stop` capability path.
- Closed the "contrast, OS scaling" gap in the WCAG 2.2 AA pass with an evidence-based finding rather than leaving it unverified indefinitely: `MainWindow` and every child widget apply zero custom stylesheets/palettes, and no code disables Qt's default HiDPI scaling, so both properties fully inherit native OS theming (including Windows' High Contrast mode). Discovered along the way that the headless test platform's palette is not a stand-in for the real one -- it substitutes a generic `fusion` theme with different colors than the real desktop's native `windows11` style, whose accent color is user-configurable besides -- so the regression test added asserts the no-custom-styling invariant rather than fixed contrast numbers, which would have quietly verified the wrong platform.
- Found and fixed a real gap: `app.py` and `EventOrchestrator` each independently built a bare default `PolicyContext()`, so the fully-implemented, unit-tested locked-screen mutation block never actually engaged in the real running app -- `locked_screen` was always `False` regardless of the actual workstation state. `visionai.runtime.build_runtime()` now builds one shared `policy_context_factory`, used by both the CLI and the orchestrator, that checks the real `WindowsLockStateAdapter` fresh on every dispatch. Verified live against the real, unlocked adapter, plus new tests proving a mutating capability is denied through the dispatcher, the orchestrator, and the CLI specifically while locked.
- Wired `JsonPermissionStore` into `build_runtime()` so stored permission grants flow into `PolicyContext.granted_capabilities` on every dispatch, and added an explicit `PolicyDecision.requires_permission` flag for UI flows that need to distinguish missing permission from other denials.
- Added a read-only Settings dialog to `MainWindow`, showing current log/data paths and the disabled raw audio/camera retention posture without changing configuration or granting permissions.
- Built the permission-grant prompt: a new `AWAITING_PERMISSION` state, a `PermissionRequest` typed event, and `EventOrchestrator.grant_permission()`/`cancel_pending_permission()` mirroring the existing confirmation flow. Granting writes to `JsonPermissionStore` and then re-evaluates the same request fresh -- if the capability also needs confirmation, that raises a `ConfirmationRequest` instead of executing directly, so a grant alone can never authorize something that still needs confirmation. `MainWindow` shows a "Grant permission" prompt the same way it shows the confirmation one.
- Added `system.clear_history`, the first built-in sensitive capability. It clears local audit history only after both permission and confirmation pass, and the dispatcher writes a new audit event afterward so the clear action remains visible.
- Live-verified the real `system.clear_history` desktop prompt flow on Windows: a genuine "Grant permission" dialog, then a separate genuine "Confirm action" dialog, then a real audit clear with the history list correctly refreshing to one entry. Found and fixed a real gap this surfaced: both dialogs originally showed the generic "Run system.clear_history." instead of a clear description of the effect, since no test asserted on the literal prompt text. `TextCommandPlanner` now gives `system.clear_history` its own summary ("Clear the local audit history.").
- Added `UserSettingsStore`, an editable Settings dialog (log level, microphone device), and a one-time onboarding dialog summarizing the safety model. Fixed a real gap this surfaced: neither entry point actually called `configure_logging()` with the stored override, so an edited log level had no effect on real output until this was wired in.
- Added cancellation-token plumbing through every capability handler (`CapabilityHandler(request, cancellation)`), so `system.stop`/the Stop button can signal cancellation a handler can actually observe, even though today's built-in handlers are all fast/synchronous and do not yet poll it.
- Added `InputAdapter`, the first voice/gesture event boundary: publishes already-recognized transcripts and already policy-approved gestures onto the runtime bus, and can invoke an injected STT provider or feed a raw gesture candidate through temporal voting.
- Added `TemporalGestureRecognizer`: a deterministic, injected-clock voting gate requiring a gesture/hand to hold steady before it votes, with a per-gesture cooldown, so a jittery or single-frame misclassification can never reach an action on its own.
- Added `PushToTalkRunner`, the smallest push-to-talk control boundary around the injected STT hook (`press()`/`release()`), and the camera/landmark adapter boundary (`GestureCandidate`, `LandmarkAdapter`) plus `GestureCaptureLoop`, which reads one frame candidate through temporal voting onto the bus.
- Added real microphone device enumeration and capture (`sounddevice`/PortAudio, the new `voice` optional dependency group) and `MicrophonePushToTalk`, bridging real press/release recording into the existing injected-STT publish path; live-verified real device enumeration and non-zero audio capture on the development machine.
- Added `visionai --list-microphones` and desktop microphone-device selection in the Settings dialog, persisted through `UserSettingsStore` and connected to `MicrophonePushToTalk`'s default capture.
- Added a wake-word activation boundary alongside push-to-talk: `WakeWordGate` deterministically strips a configured wake word (default `"visionai"`) from an already-transcribed utterance, and `WakeWordVoiceRunner` wires it to the same injected-STT publish path. Editable and persisted through `UserSettingsStore`. Text-matching only -- no real continuous capture or hotword-spotting engine yet, and not wired into the console or desktop entry points.
- Added wake-word editing to the desktop Settings dialog. The validated, normalized value is persisted through `UserSettingsStore`; continuous listening and hotword spotting remain future work.
- Added `WakeWordListeningLoop`, an injectable async transcript loop that routes accepted wake-word commands and stops cooperatively through `CancellationToken`; STT and hardware remain separate boundaries.
- Added `visionai --wake-word-text` to exercise one already-transcribed wake-word command through the real orchestrator and dispatcher; unmatched text is rejected without execution.
- Added the first real `LandmarkAdapter`: `visionai.platform.webcam.WebcamLandmarkAdapter` reads one OpenCV webcam frame and classifies it with mediapipe's offline `solutions.hands` API (pinned to `mediapipe==0.10.14`; later releases drop that API from their Windows wheels -- see `docs/DECISIONS/0003-accepted-protobuf-cve.md`). Recognizes two gestures for this first slice, `open_palm` and `closed_fist`, by counting extended fingers via a pure, independently-testable `classify_finger_count()` function. Not yet wired into a CLI/desktop surface or continuous capture loop, and camera frames/landmarks are never retained.
- Added `visionai --gesture-frames N` to capture up to N real webcam frames and report the first confirmed gesture through `GestureCaptureLoop`'s temporal voting; live-verified with a real human hand, including that a debug capture confirmed one initially-failed attempt was a camera framing issue, not a classifier bug. Observation only -- gestures still are not mapped to any capability.
- Added `visionai.recognition.GestureListeningLoop`, a continuous, cancellable version of `GestureCaptureLoop` mirroring `WakeWordListeningLoop` (`cancellation` is required rather than optional, since a real camera has no natural stream-exhausted end). Not yet wired into a CLI/desktop surface. Also investigated and ruled out an earlier ~2s/frame mediapipe latency reading from live verification -- isolated timing showed normal per-frame cost, so it was a measurement artifact of that specific run, not a real characteristic.
- Added `visionai --gesture-listen`, wiring `GestureListeningLoop` into a real CLI surface for the first time: runs on a worker thread so `Ctrl+C` can cancel cleanly (mirroring the desktop Stop button's off-GUI-thread pattern) instead of aborting mid-frame, and reports the confirmed-gesture count once stopped.
- Expanded `classify_finger_count()` from two gestures to six (`open_palm`, `closed_fist`, `thumbs_up`, `peace_sign`, `index_finger_up`, `two_fingers`) and mapped four of them to capability commands in `EventOrchestrator.process_event()`, closing the remaining gap in Phase 5's approved scope: a confirmed gesture now dispatches through the same planner/policy/dispatcher path as a typed command (recognition still carries no authority of its own). `closed_fist` is intentionally left unmapped, reserved for a future voice-mode trigger. `--gesture-listen` now stops itself on an `open_palm` gesture, with no `Ctrl+C` needed.
