# Dependencies for subsystems not yet implemented (Phase 3+).
# Pin exact versions and verify licence/maintenance status before enabling
# each block, per docs/DECISIONS/.
#
# UI (Phase 2) is now implemented -- see requirements/ui.txt for PySide6.

# --- Voice (Phase 3) ---
# Device selection/capture (sounddevice, numpy) now implemented -- see
# requirements/voice.txt and visionai.platform.microphone. STT/TTS remain
# out of scope: InputAdapter/PushToTalkRunner take an injected transcriber
# rather than bundling a specific engine.
# faster-whisper==1.0.*      # Whisper-compatible local STT
# pyttsx3==2.90              # offline TTS

# --- Vision (Phase 5) ---
# Webcam capture/hand-landmark classification (opencv-contrib-python,
# mediapipe, numpy) now implemented -- see requirements/vision.txt and
# visionai.platform.webcam. mediapipe is pinned exactly, not ranged: newer
# releases drop the offline `solutions.hands` API this module depends on.

# --- Intelligence (Phase 6) ---
# Provider boundary + one real cloud provider (Anthropic, official SDK) now
# implemented -- see requirements/intelligence.txt and
# visionai.intelligence. `visionai --ask` is conversation-only: no
# structured action planning, clarification, or conversation
# retention/memory yet, and no OS-keychain secret storage (env var only,
# same as every other secret-shaped value in this project today). A
# local/offline provider remains out of scope; `LLMProvider` is
# provider-agnostic so one can be added later with no interface change.
